[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[build]
  publish = "."
  functions = "functions"

[functions]
  node_bundler = "esbuild"

[functions."game-summary"]
  timeout = 26
